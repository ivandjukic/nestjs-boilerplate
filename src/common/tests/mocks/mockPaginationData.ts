import { PaginationDto } from '../../dtos/pagination.dto'

export const mockPaginationData: PaginationDto = {
    per_page: 20,
    total_pages: 1,
    total_items: 1,
    current_page: 1,
    previous_page: null,
    next_page: null,
}
